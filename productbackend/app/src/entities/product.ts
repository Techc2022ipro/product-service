export type Product = {
    pid: number
    name: string
    brand: string
    description: string
    seller: string
}

export type User = {
  uid: number,
  username: string,
  email: string,
  role: string
}
